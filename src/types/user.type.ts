export interface UserType {
    _id: string;
    email: string;
    full_name: string;
    avatar_url?: string;
    googleId?: string;
    role?: string;
    createdAt?: string;
    updatedAt?: string;
}