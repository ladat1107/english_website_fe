/**
 * Khailingo - Admin Class Schedule Layout
 * Layout cho quản lý lịch học (Admin)
 */

export default function AdminClassScheduleLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
